# guardrails Specification

## Purpose

定义 harness 运行时的护栏（guardrails）能力：在 agent 循环与工具分发周围施加防失控与安全约束——LLM 调用的超时与有限次退避重试、副作用工具不被重试、单次请求的 token 预算上限、原地打转检测、以及危险操作的权限闸门。全部护栏可在不依赖真实 API key、不触网的前提下做确定性单元测试，覆盖成功、失败、降级与拒绝路径。

## Requirements

### Requirement: LLM 调用超时与重试

harness SHALL 提供一个护栏 helper，对 LLM 的异步调用施加超时与有限次指数退避重试：单次调用超过配置的超时即视为失败；对超时与瞬时连接类异常 MUST 重试，重试间隔按指数退避（base × 2^n）增长，最多 `max_attempts` 次。退避等待 MUST 可注入（测试可用 no-op），不得在单元测试中产生真实等待。重试耗尽后 MUST 抛出一个明确的护栏异常（而非原始底层异常冒泡），由调用方转为优雅降级。

#### Scenario: 首次成功不重试
- **WHEN** 被包裹的 LLM 调用在超时内成功返回
- **THEN** helper 返回其结果，且未发生任何重试或退避等待

#### Scenario: 瞬时失败后重试成功
- **WHEN** 被包裹的 LLM 调用前若干次抛出可重试异常、随后成功
- **THEN** helper 按指数退避重试并最终返回成功结果，重试次数不超过 `max_attempts`

#### Scenario: 超时触发重试
- **WHEN** 被包裹的 LLM 调用耗时超过配置超时
- **THEN** helper 判定该次失败并按策略重试

#### Scenario: 重试耗尽抛护栏异常
- **WHEN** 被包裹的 LLM 调用持续失败直到用尽 `max_attempts`
- **THEN** helper 抛出明确的护栏异常，不让原始底层异常直接冒泡

### Requirement: 副作用工具不被重试

LLM 调用重试护栏 SHALL 只施加于（只读、幂等的）LLM 推理调用。带副作用的工具调用——尤其 `create_appointment`——MUST NOT 被该重试逻辑包裹，以避免因重试而重复执行写操作（如重复下单）。

#### Scenario: 工具调用不经 LLM 重试护栏
- **WHEN** 一次工具分发（如 `create_appointment`）失败
- **THEN** 系统不对该工具调用自动重试，失败按既有错误回灌路径处理，绝不重复执行其副作用

### Requirement: 循环 token 预算上限

harness SHALL 为单次请求的 agent 循环维护一个累计 token 预算（基于消息体量的近似估算即可），并接受一个 `max_tokens` 上限。当累计估算超过上限时，循环 MUST 停止并返回安全兜底回复，而非继续发起 LLM 调用。预算为防失控上限，不要求精确计费。

#### Scenario: 预算耗尽时终止
- **WHEN** 循环累计估算 token 超过配置的 `max_tokens` 上限
- **THEN** 循环停止、不再发起新的 LLM 调用，并产出安全兜底回复

#### Scenario: 预算充足时不干预
- **WHEN** 单次请求在预算内完成
- **THEN** 预算护栏不改变循环的正常行为

### Requirement: 打转检测

harness SHALL 检测循环"原地打转"：当连续若干次（达到配置的 `repeat_limit`）出现完全相同的工具调用（相同工具名与相同参数）时，MUST 判定为打转并停止循环、返回安全兜底回复，作为早于 `max_steps` 的逃生出口。

#### Scenario: 连续相同工具调用触发终止
- **WHEN** LLM 连续 `repeat_limit` 次返回名称与参数都完全相同的工具调用
- **THEN** 循环判定打转并停止，产出安全兜底回复

#### Scenario: 参数不同不算打转
- **WHEN** 相邻步骤调用同一工具但参数不同
- **THEN** 不触发打转检测，循环正常继续

### Requirement: 危险操作权限闸门

harness SHALL 提供一个可注入的权限策略机制：策略接收（工具、入参）并返回放行或拒绝（含理由）的结构化决定。被标记为危险（dangerous）的工具在分发前 MUST 先经策略判定；被拒绝时 MUST NOT 执行其 handler，而是把结构化拒绝结果交回调用方（经错误回灌路径喂给模型）。未配置策略时，默认放行，保持既有行为。

#### Scenario: 危险工具被策略拒绝
- **WHEN** 配置了一个拒绝 `create_appointment` 的权限策略，且模型请求调用它
- **THEN** 系统不执行该工具的 handler，返回带拒绝理由的结构化结果，不产生任何副作用

#### Scenario: 危险工具被策略放行
- **WHEN** 权限策略对某次危险工具调用返回放行
- **THEN** 系统正常执行其 handler 并返回结果

#### Scenario: 默认无策略时放行
- **WHEN** 未注入任何权限策略
- **THEN** 危险工具按既有行为正常执行，不被拦截

### Requirement: 护栏离线确定性可测

全部护栏（超时/重试、预算、打转、权限）SHALL 可在不依赖真实 API key、不触网的前提下做确定性单元测试：通过注入 fake LLM、可控异常、no-op 退避等待与显式策略对象覆盖成功、失败、降级与拒绝路径。

#### Scenario: 注入异常驱动降级路径
- **WHEN** 用注入受控超时/异常的 fake 依赖运行护栏
- **THEN** 测试确定性地观察到重试、终止或拒绝行为，全程不发起真实网络调用

### Requirement: 外部调用超时与非阻塞

除 LLM 之外的外部 I/O 调用（当前为嵌入/向量化）SHALL 施加可配置的请求超时上限，缺省值 MUST 为秒级量级而非分钟级。超时 MUST 由 HTTP 客户端自身承担，MUST NOT 仅依赖调用处的外层包装（如 `asyncio.wait_for`）。

在 `async` 上下文中 MUST NOT 执行同步阻塞 I/O。此类调用 SHALL 走 provider 的原生异步方法，使其可被取消。

两条约束缺一不可，因为它们防的是**两个不同的缺陷**：

- 只有客户端超时、调用仍是同步的：请求会在上限内返回，但这段时间里**整个事件循环是冻住的**——同进程内所有协程（含 IM 长连接的收包与心跳）一并停摆。
- 只有异步化、没有客户端超时：事件循环能继续推进，但那次调用可能**永不返回**，连接与任务无限期泄漏。

「超时不得只依赖外层包装」是从实测缺陷中反推出的硬约束：`asyncio.wait_for` 只能中断在 await 点让出控制权的协程。若被包裹的调用是同步阻塞的，事件循环连超时定时器的回调都无法执行——外层超时形同不存在，却给人「已经有超时了」的错觉。这与既有需求「LLM 调用超时与重试」互补：那条管的是 LLM，本条管其余外部调用。

超时后 SHALL 由调用方按既有的失败路径降级（如检索失败回灌给模型），MUST 记录可排查的错误日志，MUST NOT 静默返回空结果而不留痕。

#### Scenario: 外部调用有秒级超时上限

- **WHEN** 检查嵌入客户端的构造
- **THEN** 其请求超时已被显式设置为可配置的秒级数值，而非沿用底层客户端的分钟级默认值

#### Scenario: 永不返回的调用被按时掐断

- **WHEN** 嵌入服务的请求永不返回
- **THEN** 该调用在配置的超时内以失败收场，不无限期挂起

#### Scenario: 等待期间事件循环仍可推进

- **WHEN** 一次嵌入调用正在等待响应
- **THEN** 同一事件循环上的其它协程仍按时被调度执行，不被该调用阻塞

#### Scenario: async 上下文不含同步阻塞 I/O

- **WHEN** 审查 `async` 方法中的嵌入调用
- **THEN** 它们调用的是原生异步接口，不存在直接调用同步阻塞版本的情形

#### Scenario: 超时可被离线确定性测试

- **WHEN** 注入一个永不返回的假嵌入实现运行测试
- **THEN** 测试在超时内断言失败行为，不触网、不产生真实等待到分钟级

#### Scenario: 超时留下可排查的记录

- **WHEN** 嵌入调用因超时失败
- **THEN** 记录错误日志且调用方按既有失败路径降级，不静默返回空结果

### Requirement: 多候选向量化 SHALL 异步并发

需要对多个候选文本做向量化的相似度匹配（当前为技师专长匹配）SHALL 走 provider 的原生异步方法，并 SHALL **并发**发起各候选的向量化请求，MUST NOT 串行逐个 `await`。

本需求是「外部调用超时与非阻塞」在具体调用点上的落地锚：那条需求只在抽象层面禁止同步阻塞，没有任何场景绑到实际路径，这正是技师专长匹配能长期违规而无人察觉的原因（`fix-embedding-timeout-blocking` 只修了知识库那条链，技师这条漏了一个多月）。

并发不只是性能优化：串行 `await` 虽然不冻结事件循环，但会把延迟放大到 `N × RTT`，在 N 个候选、每次上限秒级的前提下，单次工具调用可能超过 agent loop 的工具超时而被整体掐断——**功能上等价于失败**。

向量化结果的顺序 MUST 与候选列表顺序严格对应，使相似度排序结果不因并发而改变。

#### Scenario: 候选向量化期间事件循环仍能推进

- **WHEN** 在 `async` 上下文中触发技师专长相似度匹配，且向量化请求长时间不返回
- **THEN** 同时被调度的其他协程 MUST 继续按时推进（心跳不停），MUST NOT 因同步阻塞而全部停摆

#### Scenario: 多候选并发而非串行

- **WHEN** 对 N 个候选执行相似度匹配
- **THEN** 各候选的向量化请求并发发起，整体耗时约为单次请求的量级，MUST NOT 呈 N 倍串行累加

#### Scenario: 并发不改变排序结果

- **WHEN** 给定固定的候选列表与确定性的向量化实现
- **THEN** 返回的候选索引序列与串行实现完全一致（结果顺序按候选下标对应，不按请求完成顺序）

#### Scenario: 挂起的匹配可被取消

- **WHEN** 相似度匹配正在等待向量化结果时其调用被取消（如触发工具超时）
- **THEN** 该调用 MUST 真正终止，MUST NOT 出现"外层已返回、底层请求仍在后台跑完"的线程与连接泄漏
